---

# This keeps the original roles active. Keep it set to true
# for now. See roles/mythtv/tasks/main.yml for detail.
use_old_roles: true

# These are for the Python Virtual Environment:
venv_path: /usr/local/dist
venv_active: false

...

# vim: set expandtab tabstop=2 shiftwidth=2 smartindent noautoindent colorcolumn=6:
