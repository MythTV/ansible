---
DOCUMENTATION:
  name: mythtv.general.yet_another_filter
  plugin_type: filter
  short_description: Dummy yml file for another filter
  description:
    - Put something meaningful here. Right now, it just echos the input.
  options:
    _input:
      description: 'explain _input, also a test of the "required: true" setting'
      type: string
      default: whatever
      required: true

EXAMPLES: |
  - name: Some name for a task
    ansible.builtin.debug:
      var: '{{ some_file_name | mythtv.general.yet_another_filter() }}'

  - name: Blah blah blah blah
    ansible.builtin.debug:
      var: '{{ some_file_name | mythtv.general.yet_another_filter() }}'

RETURN:
  _output:
    description: the _output description
    type: bool
    returned: always
    sample: put a real sample here

# vim: set expandtab tabstop=2 shiftwidth=2 smartindent noautoindent colorcolumn=80:
