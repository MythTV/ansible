# roles/mythtv-deb/tasks/main.yml

---

- name: create a list of deb compilers and build essentials
  set_fact:
    deb_pkg_lst:
      - git
      - g++
      - make
      - build-essential
      - nasm
      - automake
      - libtool
      - ccache
      - pkg-config

- name: add mythtv essential build libraries
  set_fact:
    deb_pkg_lst:
      - '{{ deb_pkg_lst }}'
      - uuid-dev
      - libfreetype6-dev
      - libmp3lame-dev
      - libxv-dev
      - libxxf86vm-dev
      - libxinerama-dev
      - libxrandr-dev
      - libxml2-dev
      - libavahi-compat-libdnssd-dev
      - libexiv2-dev
      - libasound2-dev
      - libegl1-mesa-dev
      - liblzo2-dev
      - libhdhomerun-dev
      - libsamplerate0-dev
      - libxnvctrl-dev
      - libzip-dev
      - libsoundtouch-dev

- name: add mythtv extra build libraries
  set_fact:
    deb_pkg_lst:
      - '{{ deb_pkg_lst }}'
      - libva-dev
      - libdrm-dev
      - libvdpau-dev
      - libass-dev
      - libxvidcore-dev
      - libx264-dev
      - libvpx-dev
      - libbluray-bdj
      - libavc1394-dev
      - libiec61883-dev
      - libpulse-dev
      - libfftw3-dev
      - libssl-dev
      - libsystemd-dev
      - libgnutls28-dev

- include_tasks: "{{ ansible_distribution | replace(' ', '_') | lower }}.yml"

# - include_tasks: debian.yml
#   when: ansible_distribution == "Debian"
#
# - include_tasks: ubuntu.yml
#   when: ansible_distribution == "Ubuntu"
#
# - include_tasks: linux_mint.yml
#   when: ansible_distribution == "Linux Mint"

- name: add mythtv essential perl modules
  set_fact:
    deb_pkg_lst:
      - '{{ deb_pkg_lst }}'
      - libdbi-perl
      - libdbd-mysql-perl
      - libnet-upnp-perl
      - libwww-perl
      - libio-socket-inet6-perl
      - libdate-manip-perl
      - libxml-simple-perl
      - libxml-xpath-perl
      - libimage-size-perl
      - libdatetime-format-iso8601-perl
      - libsoap-lite-perl
      - libjson-perl

- name: add mythtv essential plugin libraries
  set_fact:
    deb_pkg_lst:
      - '{{ deb_pkg_lst }}'
      - libvorbis-dev
      - libflac-dev
      - libflac++-dev
      - libtag1-dev
      - libcdio-dev
      - libcdio-paranoia-dev

- name: final package list
  debug:
    msg:
      '{{ lookup("flattened", deb_pkg_lst) }}'

- name: install packages
  apt:
    name:
      '{{ lookup("flattened", deb_pkg_lst ) }}'
    state:
      present

...

# vim: set expandtab tabstop=2 shiftwidth=2 smartindent noautoindent colorcolumn=2:
